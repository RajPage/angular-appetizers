import { SecsToTimePipe } from './secs-to-time.pipe';

describe('SecsToTimePipe', () => {
  it('create an instance', () => {
    const pipe = new SecsToTimePipe();
    expect(pipe).toBeTruthy();
  });
});
